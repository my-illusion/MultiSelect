const a = 1;

export const add = (num1: number, num2: number): number => {
  return num1 + num2;
};

const str = [1, 2, 3];
const result = [...str.map((i) => i + 1)];

console.log(result);

export default a;
